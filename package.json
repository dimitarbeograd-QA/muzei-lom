{
  "name": "muzei-lom",
  "private": true,
  "version": "1.0.0",
  "description": "Исторически музей — Лом (E2E тестове)",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
